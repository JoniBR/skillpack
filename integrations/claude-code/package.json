{
  "name": "@skill-pack/claude-plugin",
  "version": "0.1.0",
  "description": "Claude Code plugin for skill-pack: contributes the /skillpack slash command + meta-skills.",
  "license": "MIT",
  "private": false,
  "publishConfig": { "access": "public" },
  "files": [
    "commands/**/*",
    "README.md"
  ],
  "scripts": {
    "lint": "echo \"(lint: no-op)\"",
    "typecheck": "echo \"(no ts here)\"",
    "test": "echo \"(no tests; see CLI package)\"",
    "build": "echo \"(static plugin; nothing to build)\""
  }
}
